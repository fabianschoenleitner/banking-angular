import {AddTokenInterceptor} from './add-token-interceptor';

describe('AddTokenInterceptor', () => {
  it('should create an instance', () => {
    expect(new AddTokenInterceptor()).toBeTruthy();
  });
});
